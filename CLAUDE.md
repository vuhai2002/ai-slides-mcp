# CLAUDE.md

Project guidance for AI agents lives in @AGENTS.md - read it first.

This file intentionally only points to `AGENTS.md` so the same guidance serves
every AI tool (Claude Code, Codex, Cursor, ...). Put project-specific rules in
`AGENTS.md`, not here.
